# copy to .env and fill in real values

# postgres connection string (supabase pooler URL)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# any long random string
SESSION_SECRET=changeme-long-random-string

# server port
PORT=3000
